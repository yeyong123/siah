#encoding:utf-8
<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "说说现在发生的事..." %>
	</div>
	<%= f.submit "发布", class: "btn btn-large btn-primary" %>
<% end %>

