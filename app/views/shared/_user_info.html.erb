#encoding:utf-8
<a href="<%= user_path(current_user) %>">
	<%= gravatar_for current_user, size: 51 %>
</a>
<h1>
	<%= current_user.name %>
</h1>
<span>
	<%= link_to "查看个人资料", current_user %>
</span>
<span>
	<%= current_user.microposts.count %> 微博
</span>
